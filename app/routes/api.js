var bodyParser	= require('body-parser'); //TODO: ovaj call verovatno nece trebati
var jwt 		= require('jsonwebtoken');

var User 		= require('../models/user');
var config 		= require('../../config');

module.exports = function(app, express){
	
};